<!--logs.wxml-->

<view wx:if="{{array.length > 5}}">too much</view>
<view wx:elif="{{array.length > 2}}"> <view wx:for="{{array}}">
  <text>第{{index+1}}</text>: <image src="{{item.message}}"></image>
</view>
</view>
<view wx:else> Error</view>
